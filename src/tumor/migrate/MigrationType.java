
package tumor.migrate;

/**
 * Enumerates the types of migration models.
 */
public enum MigrationType {
    /**
     * All tumor components are fixed (pinned in place) and do not
     * migrate.
     */
    PINNED;
}
